import React, { Component } from 'react'
import { render } from 'react-dom'

export default class Main extends Component {
  render() {
    return (
      <div>hello111cdcdc {this.props.name}</div>
    )
  }
}
